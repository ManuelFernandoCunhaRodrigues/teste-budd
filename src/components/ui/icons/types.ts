import { colors } from '@/theme';

/** Shared props for every icon in this set. */
export interface IconProps {
  size?: number;
  color?: string;
}

export const DEFAULT_ICON_SIZE = 20;
export const DEFAULT_ICON_COLOR = colors.text;
